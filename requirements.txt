# ============================================================
# AIDSE — Python runtime dependencies
#
# Pinned exactly, not with >=. This app ships as a PyInstaller bundle
# built from whatever these resolve to, so a floating range means the
# installer users download was built against versions nobody tested. The
# versions below are the ones the suite passes on.
#
# Development tools live in requirements-dev.txt; the release workflow
# installs only this file.
#
# To move a pin: change it, run the suite, rebuild the sidecar, commit.
# Python 3.11–3.14.
# ============================================================

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# Database
sqlalchemy[asyncio]==2.0.52
alembic==1.19.1
aiosqlite==0.22.1

# Validation
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0

# Auth / Security
bcrypt==5.0.0
python-jose[cryptography]==3.5.0
cryptography==49.0.0
# OS-native credential store for the per-install app secret (apps/api/db/vault.py).
# Without it the vault falls back to a 0600 file and logs a warning.
keyring==25.7.0

# Rate limiting
slowapi==0.1.10

# HTTP client. Not test-only, whatever this file used to say: evaluation
# scoring and runner, the conversational service and dataset
# recommendations all call out through it.
httpx==0.28.1

# Utilities
python-dotenv==1.2.3
orjson==3.12.0

# Machine Learning & AutoML
scikit-learn==1.9.0
xgboost==3.4.1
lightgbm==4.7.0
catboost==1.2.10
optuna==4.9.0
# Experiment tracking. Its only user-visible output today is the run id
# column in the experiments table, for ~996 bundled submodules; kept
# deliberately, worth revisiting.
mlflow==3.15.2

# Data handling (dataset ingestion & profiling)
pandas==2.3.3
numpy==2.5.2
openpyxl==3.1.5
xlrd==2.0.2

# Explainability
shap==0.52.0
