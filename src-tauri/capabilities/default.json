{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Explicit allow-list permissions for AIDSE Desktop application shell",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-sidecar",
    "process:allow-restart",
    "process:allow-exit"
  ]
}
