[package]
name = "aidse-desktop"
version = "0.1.0"
description = "AIDSE Desktop — AI Data Scientist & Evaluation Platform"
authors = ["AIDSE Team"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-shell = "2"
tauri-plugin-process = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
log = "0.4"
# Cryptographically strong token shared only between this shell and the
# sidecar it spawns.
rand = "0.8"

# Update checks against GitHub Releases. Kept out of the main dependency table
# because the updater plugin has no mobile implementation, and a target-gated
# dependency documents that rather than failing a build nobody runs.
[target."cfg(not(any(target_os = \"android\", target_os = \"ios\")))".dependencies]
tauri-plugin-updater = "2"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
